package a.alt.z.weather.ui.main

import a.alt.z.weather.databinding.ItemWeeklyForecastBinding
import a.alt.z.weather.model.WeeklyForecast
import androidx.recyclerview.widget.RecyclerView

class WeeklyForecastViewHolder(
    private val binding: ItemWeeklyForecastBinding
) : RecyclerView.ViewHolder(binding.root) {

    fun bind(weeklyForecast: WeeklyForecast) {
        binding.apply {

        }
    }
}